// const express = require("express");
// const router = express.Router();
// const studentController = require("../../controllers/studentController");

// router.get("/", studentController.getAllStudents);

// module.exports = router;
// // this file is part of the Express.js backend for managing student data in a gradebook application.
// // It defines a route to fetch all students from the database using the studentController.
// // The route is set up to respond to GET requests at the "/api/student" endpoint.
// // The studentController is responsible for handling the logic of fetching students,
// // which is expected to return a list of students in JSON format.